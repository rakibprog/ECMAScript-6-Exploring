// template string    this is `` caret symbol
const firstName = 'rocky';
const lastName = 'khan';
const fullName = `${firstName} ${lastName}`;
console.log(fullName);

// another way  multiline string 

const paraGraph = `hello every one , my daily life is very simple 
    i get up early in the morning around 6 pm 
    then i go to wash room ,
    and i washes my hand and faces.
`
console.log(paraGraph);

// Expression add 

const num1 = 10;
const num2 = 20;
const total = `Total: ${(num1+num2) * 2}`;
console.log(total);